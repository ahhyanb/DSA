/*
create a seen variable to through the string array 
create a maxCount variable to keep track of the max count
create a left to remove characters from the seen when we see a duplicate
go through the string
    for each char 
        check if the char is in seen
            if its not put it in seen 
            update the the count to if the count is more than the max
            if it is, 
            using the left pointer go through the seen and delete characters until you get to the duplicate

return maxcount

*/


/*

*/
